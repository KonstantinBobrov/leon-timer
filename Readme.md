# PostgreSQL Docker container Run
For running Postgres db please run 'docker compose up -d'

# Application Run 
For running application please run
./gradlew bootRun

# API endpoint
http://localhost:8080/api/timer